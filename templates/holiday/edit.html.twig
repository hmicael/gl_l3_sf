{% extends 'base.html.twig' %}

{% block title %}Edit Holiday{% endblock %}

{% block body %}
    <section class="col-md-6 align-center">
        <h1>Edit Holiday</h1>

        {{ include('holiday/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_general_constraints_index') }}" class="mt-3 mb-3 btn btn-outline-secondary">
            <i class="fa-solid fa-arrow-left"></i> Back
        </a>

        {{ include('holiday/_delete_form.html.twig') }}
    </section>
{% endblock %}
